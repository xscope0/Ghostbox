/**
 * config.example.js — Template for config.js
 *
 * Copy this file to config.js and fill in your Supabase credentials:
 *   cp js/config.example.js js/config.js
 *
 * SECURITY: The anon key is safe for client-side use. NEVER use a
 * service_role key here — it would expose full database access.
 * Security relies on Supabase Row-Level Security (RLS) policies.
 */

// ── Supabase credentials ──

export const SB_URL = 'https://YOUR_PROJECT_ID.supabase.co';
export const SB_ANON_KEY = 'YOUR_ANON_KEY_HERE';

// ── Rest of config continues below (copy from config.js) ──
